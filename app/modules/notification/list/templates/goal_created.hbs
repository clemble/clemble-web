<hr/>
<div class="container-fluid">
	<div class="row">
		<div class="col-md-2">
			<div>
				{{smallProfileImage player}}
			</div>
			<h4><label class="label label-success">{{formatDate startDate "%d/%m"}}</label></h4>
		</div>
		<div class="col-md-10">
			<h5>Created</h5>
			<h4>{{goal}}</h4>
			{{#unless my}}
				{{#if myBid}}
					My: {{bid myBid}}
				{{else}}
				{{/if}}
			{{/unless}} Total: {{bid bank.total}}
		</div>
	</div>
</div>

