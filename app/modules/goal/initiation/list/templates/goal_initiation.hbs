<hr/>
<div class="container-fluid">
	<div class="col-md-12">
		<button id="confirm" class="btn btn-circle btn-success"><span class="fa fa-rocket"></span></button>
		<div data-countdown="{{startDate}}"></div>
		{{goal}}<br/>
		{{bid bank.total}}<br/>
		<span class="badge">{{length observers}}</span> Observers<br/>
		<span class="badge">{{length supporters}}</span> Supporters 
	</div>
</div>
