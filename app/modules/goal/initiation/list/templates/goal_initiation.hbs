<hr/>
<div class="container-fluid">
	<div class="col-md-12">
		<button id="confirm" class="btn btn-circle btn-success"><span class="fa fa-rocket"></span></button>
		<div data-countdown="{{startDate}}"></div>
		{{goal}}<br/>
		{{bid bank.total}}<br/>
		<span class="badge">{{length supporters}}</span>
		{{#each supporters}}{{microProfileImage this}}{{/each}}
	</div>
</div>
